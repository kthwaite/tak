Implemented workspace scaffold:
- Added `[workspace]` members in root `Cargo.toml` for `crates/tak-core` and `crates/tak-cli`.
- Added `crates/tak-core` lib scaffold (`workspace_scaffold_marker`).
- Added `crates/tak-cli` bin scaffold with path dependency on `tak-core`.
- Verified compile entrypoints remain intact:
  - `cargo test -p tak-core --no-run`
  - `cargo test -p tak-cli --no-run`
  - `cargo test -p tak --no-run`

Note: `Cargo.lock` updated with workspace package entries for new crates.