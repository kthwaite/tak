Completed extraction feature scope:

- 1175f88d903de690: workspace scaffold (`crates/tak-core`, `crates/tak-cli`, workspace members)
- dfda94f3d815ef6b: moved reusable internals into `tak-core` and made root `tak` lib a re-export shim
- ac8fb647c2e5157b: added `tak-core` facade module (repo context, task-id helpers, coordination verbosity helpers, process-output/error hooks) and delegated root CLI helper flows to it

Verification evidence:
- `cargo test -p tak-core facade::tests`
- `cargo test -p tak --no-run`
- `cargo test -p tak-cli --no-run`
- `cargo test --workspace --no-run`

All checks pass post-`cargo fmt`.