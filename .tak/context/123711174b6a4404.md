Expanded parity/coverage matrix for workspace split with new tak-cli-focused tests:

- Added `crates/tak-cli/tests/parity_smoke.rs`:
  - `json_create_show_and_list_roundtrip` validates init/create/show/list behavior and canonical ID shape via `tak-cli` binary.
  - `json_error_envelope_is_emitted_on_failures` validates structured JSON stderr error envelope when command fails.

- Added `crates/tak-cli/tests/guardrails.rs` (architectural parity safeguard):
  - enforces thin-wrapper shape for `crates/tak-cli/src/main.rs`
  - enforces no direct `clap` dependency in `crates/tak-cli/Cargo.toml`

- Updated `crates/tak-cli/Cargo.toml` dev-dependencies (`serde_json`, `tempfile`) to support smoke tests.

Verification:
- `cargo test -p tak-cli --tests`
- `cargo test -p tak --no-run`
- `cargo test --workspace --no-run`