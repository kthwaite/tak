Wired `crates/tak-cli` to parse and dispatch via `tak-core` APIs:

- Replaced placeholder `crates/tak-cli/src/main.rs` with full CLI parser/dispatch implementation adapted from root main, switching crate references from `tak::` to `tak_core::`.
- Added required `tak-cli` dependencies for parser/runtime/tests (`chrono`, `clap`, and `tempfile` dev-dependency) in `crates/tak-cli/Cargo.toml`.
- Dispatch path in `tak-cli` now calls `tak_core` command/facade surfaces (including task-id resolution + error rendering hooks through `tak_core::facade`).

Validation:
- `cargo test -p tak-cli --no-run`
- `cargo test -p tak --no-run`
- `cargo test --workspace --no-run`

All compile checks pass after `cargo fmt`.