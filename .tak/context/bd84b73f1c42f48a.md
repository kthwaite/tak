Implemented thin-wrapper responsibility split for `tak-cli`:

- Moved full clap parser + command dispatch implementation into core as `crates/tak-core/src/cli.rs`.
- Exported `tak_core::cli::run_cli()` from `tak-core`.
- Reduced `crates/tak-cli/src/main.rs` to a minimal process wrapper:
  - calls `tak_core::cli::run_cli()`
  - maps return code via `std::process::exit`
- Simplified `crates/tak-cli/Cargo.toml` back to only `tak-core` dependency (dropped direct `clap`, `chrono`, and test-only deps), reinforcing that parser/execution policy belongs to core.

Validation:
- `cargo test -p tak-core cli::tests --no-run`
- `cargo test -p tak-cli --no-run`
- `cargo test -p tak --no-run`
- `cargo test --workspace --no-run`

All checks pass.