Feature complete:
- 9b86d7614da698fd: `crates/tak-cli` parser/dispatch wired to `tak-core` APIs.
- bd84b73f1c42f48a: `tak-cli` reduced to IO/exit wrapper over `tak_core::cli::run_cli()` with parser+dispatch moved into core (`crates/tak-core/src/cli.rs`).
- 98a8ab37227f7ff5: guardrail tests added under `crates/tak-cli/tests/guardrails.rs` to prevent CLI business-logic creep and direct clap dependency reintroduction.

Verification:
- `cargo test -p tak-cli --tests`
- `cargo test -p tak --no-run`
- `cargo test --workspace --no-run`