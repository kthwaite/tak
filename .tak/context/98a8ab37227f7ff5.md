Added architectural guardrails to keep `tak-cli` thin:

- Added integration guardrail tests in `crates/tak-cli/tests/guardrails.rs` that assert:
  - `crates/tak-cli/src/main.rs` remains a tiny wrapper over `tak_core::cli::run_cli()`
  - no direct `clap` import appears in `tak-cli` main
  - no local command-dispatch pattern (`match cli.command`) appears in `tak-cli` main
  - `crates/tak-cli/Cargo.toml` avoids direct `clap` dependency and keeps `tak-core` dependency

Validation:
- `cargo test -p tak-cli --tests`
- `cargo test -p tak --no-run`
- `cargo test --workspace --no-run`

All checks pass.